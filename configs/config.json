{
  "mode": "train",

  "epochs": 25,
  "batch_size": 128,

  "vocab_size": 30000,
  "max_sequence_len": 80,

  "model": {
    "type": "transformer",
    "config": {
      "layers": 3,
      "embed_dim": 256,
      "num_heads": 8,
      "feed_forward_dim": 512
    }
  }

}
