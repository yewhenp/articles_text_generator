{
  "mode": "train",

  "epochs": 25,
  "batch_size": 32,

  "vocab_size": 20000,
  "max_sequence_len": 80,

  "model": {
    "type": "transformer",
    "config": {
      "layers": 5,
      "embed_dim": 512,
      "num_heads": 4,
      "feed_forward_dim": 256
    }
  }

}
